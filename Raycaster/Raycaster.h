// Raycaster.h

  namespace RaycasterDll
  {
      class Raycaster
      {
      public:
          // right now it just prints some pre-determined nonsense to stdout
          static __declspec(dllexport) void castRays();
	  };
  }